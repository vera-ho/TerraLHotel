import { connect } from "react-redux";
import FooterIndex from "./footer";

const mSTP = state => {
    return {

    }
}

const mDTP = dispatch => {
    return {

    }
}

export default connect(mSTP, mDTP)(FooterIndex);